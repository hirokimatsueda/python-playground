on:
  push:
    branches:
      - ruff-ci

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install ruff
        run: python -m pip install ruff

      - name: Lint and generate SARIF per app
        run: |
          set +e
          for app in apps/*/; do
            dir=$(basename "$app")
            output_path="app/$dir.sarif"
            ruff check "$app" --output-format sarif --output-file $output_path
            cat $output_path
          done

      - name: Upload SARIF
        uses: github/codeql-action/upload-sarif@v3
        if: success() || failure()
        with:
          sarif_file: "apps/"
